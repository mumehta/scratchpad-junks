terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
    }
    random = {
      source = "hashicorp/random"
    }
  }
}

provider "aws" {
  profile = "personal"
  region  = "ap-southeast-2"
}

provider "random" {}